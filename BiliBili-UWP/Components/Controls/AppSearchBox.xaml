<UserControl
    x:Class="BiliBili_UWP.Components.Controls.AppSearchBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BiliBili_UWP.Components.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <Flyout FlyoutPresenterStyle="{StaticResource NoPaddingFlyoutStyle}" x:Key="BasicFlyout" Placement="Bottom">
            <Grid x:Name="BasicContainer">
                
            </Grid>
        </Flyout>
    </UserControl.Resources>
    <Grid>
        <AutoSuggestBox Style="{StaticResource BasicSearchBoxStyle}" x:Name="BiliSearchBox"
                        GotFocus="BiliSearchBox_GotFocus" KeyDown="BiliSearchBox_KeyDown" CornerRadius="20"
                        PlaceholderText="搜索视频" QuerySubmitted="BiliSearchBox_QuerySubmitted"
                        />
    </Grid>
</UserControl>
